let name = prompt(`как тебя зовут`)
if (name = `Alex`) {
    let account = prompt(`номер счета`)
    if (account == `7777`) {
        let balance = + prompt(`Сколько обналичить`)
        if (balance >= `10000`) {

            let m   = (`жавобаш`)
            if (m === `-`) {
                alert(balance === balance)
                let money = (`money`) 
                if (money === true) {
                    let remainder = prompt(`Сколько снял`)
                } else {
                    alert(
                        `Не достаточно средств`
                    )
                }
            } else {
                alert(
                    `Не достаточно средств`
                )
            }
        } else {

        }
    } else {
        alert(`Пользователь не найден досвидули`)
    }

} else {
    alert(`Пользователь не найден досвидули`)


}






